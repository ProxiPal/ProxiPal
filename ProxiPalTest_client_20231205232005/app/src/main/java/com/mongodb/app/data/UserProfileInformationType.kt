package com.mongodb.app.data

/**
 * An enum class to define different types of information that can be shown and edited in the user profile screen
 */
enum class UserProfileInformationType {
    FirstName, LastName, Biography
}